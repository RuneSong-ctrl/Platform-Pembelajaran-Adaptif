# =====================================================================
# 🎓 EduAdapt Frontend Environment Configuration Template
# =====================================================================
# Panduan:
# 1. Salin file ini menjadi `.env` atau `.env.local` di folder frontend/ :
#    cp .env.example .env (Linux/macOS) atau copy .env.example .env (Windows)
# 2. Sesuaikan URL API jika backend berjalan di host/port berbeda.
# =====================================================================

VITE_APP_TITLE="EduAdapt - Platform Pembelajaran Adaptif AI & Blockchain"
VITE_APP_VERSION="0.1.0"

# URL Endpoint Backend API FastAPI
VITE_API_BASE_URL="http://localhost:8000/api/v1"
